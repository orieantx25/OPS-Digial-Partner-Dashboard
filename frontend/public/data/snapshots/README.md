# Leadership snapshots

Generated by `python backend/scripts/publish_snapshots.py` (or `npm run publish:snapshots`).

These JSON files power the Vercel leadership build (`NEXT_PUBLIC_DATA_MODE=static`).
Aggregated charts/KPIs only — not raw lead rows.
